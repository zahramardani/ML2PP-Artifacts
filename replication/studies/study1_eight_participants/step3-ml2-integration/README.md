# Step 3 — ML2++ integration

This directory contains the Stage-3 ML2++ artifact supplied for the participant study. Participant-identifying filenames are not used in this public deposit.

The supplied Stage-3 archive is deposited here as its complete unpacked source tree, preserving the original generated directory structure. See `ARCHIVE_FORMAT.md` for the archival note.
