# Use Cases

Create one directory per evaluated IoT/CPS use case. Each directory should contain its ML2++ model instance, data-source description, preprocessing configuration, forecasting setup, execution instructions, and expected results.
